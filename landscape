Região (Region)
 └── Zona de Disponibilidade (AZ / Zone)
      └── Data Centers (1 ou mais)
           └── Racks
                └── Servidores (compute + storage)
                     └── Discos (HDD/SSD)
					 

Conta AWS
 └── IAM
      ├── Identidades
      │     ├── Users
      │     └── Roles
      ├── Agrupadores
      │     └── Groups
      └── Regras
            └── Policies
            